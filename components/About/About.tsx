import React from 'react';
import { ComponentProps } from '@/types/types'

function About({darkTheme, ruLang}: ComponentProps) {
  return (
    <div>
      about
    </div>
  );
}

export default About;